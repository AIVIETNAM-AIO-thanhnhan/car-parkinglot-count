# =====================================================================
# PKLot Parking Detection — Module 3
# Cài đặt:  pip install -r requirements.txt
#
# Colab đã có sẵn: numpy, pandas, scikit-learn, opencv, scikit-image,
# matplotlib, scipy, joblib, seaborn, Pillow.
# Trên Colab chỉ cần cài phần "THIẾU TRÊN COLAB" ở cuối file:
#   !pip -q install pyarrow mahotas lightgbm optuna shap imagehash
# =====================================================================

# --- Cốt lõi: xử lý dữ liệu ---
numpy>=1.26,<3
pandas>=2.2
scipy>=1.11

# --- Xử lý ảnh & trích feature ---
opencv-python-headless>=4.9      # headless: không cần GUI, nhẹ hơn
scikit-image>=0.22               # HOG, Local Binary Pattern
Pillow>=10.0

# --- Model (Module 3) ---
scikit-learn>=1.4                # DecisionTree, RandomForest, AdaBoost

# --- Lưu cache feature ---
pyarrow>=14.0                    # BẮT BUỘC để đọc/ghi .parquet

# --- Trực quan hóa ---
matplotlib>=3.8
seaborn>=0.13                    # heatmap quét ngưỡng × NMS

# --- Tăng tốc ---
joblib>=1.3                      # Parallel khi trích feature

# =====================================================================
# TÙY CHỌN — chỉ cài khi cần, không bắt buộc cho dự án
# =====================================================================

# W3 (28-29/08) — nếu kịp học thì dùng, không thì bỏ
lightgbm>=4.3
xgboost>=2.0
optuna>=3.6

# Diễn giải model (Ngày 12)
shap>=0.44

# Feature kết cấu Haralick/GLCM — P3 dùng nếu mở rộng feature
mahotas>=1.4

# Lọc ảnh gần trùng — dự phòng nếu lấy mẫu theo thời gian chưa đủ
imagehash>=4.3

# =====================================================================
# THIẾU TRÊN COLAB (copy dòng dưới vào ô đầu notebook)
# !pip -q install pyarrow mahotas lightgbm optuna shap imagehash
# =====================================================================
